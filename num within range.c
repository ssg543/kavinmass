#include<stdio.h>
void main()
{
    int a;
    printf("Enter the number:");
    scanf("%d",&a);
    if(a>=1&&a<=10)
    {
        printf("Yes, in the range");
    }
    else
    {
        printf("No,not in range");
    }
        
        
}
